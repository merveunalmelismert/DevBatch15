integer i;
integer j =7;
integer z = i+j; // if integer is null, we cannot do any operation.
system.debug(z);


string s;
String s1 = 'this';
String s2 = s+ s1; // if string is null, we can concate. we cannot call any string methods.
system.debug(s2);