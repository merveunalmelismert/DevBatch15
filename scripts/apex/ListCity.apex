 List<string> ListCity = new List<String> {'Amsterdam', 'Istanbul','New York', 'Paris', 'Toronto'};
    Integer Citys = ListCity.size();
        system.debug('Sehirlerin sayisi'  + Citys);
        system.debug(ListCity);
        system.debug(ListCity.get(0));
        system.debug(ListCity.get(4));
    for (string tumListCity : ListCity) {
        system.debug(tumListCity + 'city');
        
    }